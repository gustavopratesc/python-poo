# explicou operadores, relacionais, logicos, atribuição, matematicos

try:
    base = float(input('Insira a base: '))
    altura = float(input('Insira a altura: '))

    area = (base + base) * altura / 2

    print(f'A area é {area}')
except ValueError:
    print('ERRO: Digite apenas números!')

    #########################

import math

angulo = 0

x1 = -1 / (math.sqrt(1) - ((10 + 3) ** 2))

x2 = (math.sqrt((4 - 3) * math.cos(angulo) (math.sqrt())))